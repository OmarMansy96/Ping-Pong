using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestInput : MonoBehaviour
{
    
    void Update()
    {
        var f =Input.mousePosition;
        var ff=Input.mousePosition;

        //var d = Input.mouseScrollDelta; if (Input.GetKey(KeyCode.RightArrow) || (Input.mousePosition.x > 0 && Input.GetMouseButton(0)))
        //{
        //    //_direction = Vector2.right;
        //    _direction = new Vector2(Input.mousePosition.x, 0);
        //}
        //else if (Input.GetKey(KeyCode.LeftArrow) || (Input.mousePosition.x < 0 && Input.GetMouseButton(0)))
        //{
         
        //}
 

    }
}
